import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.registrapp.duoc.cl',
  appName: 'RegistrApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
